/**
 * Re-exports the GET and POST authentication handlers from the auth library.
 *
 * These handlers are used by NextAuth for handling OAuth callbacks and session management.
 */
export { GET, POST } from '@/lib/auth';